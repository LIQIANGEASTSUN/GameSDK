# EventBus 模块状态

需求规则已确认，实施任务仍为规划，尚未实现、接通或验收。

## 前置条件

| 前置 | 当前缺口与可用输入 | 就绪条件 |
|---|---|---|
| 普通类单例 | [实现普通类单例](../../Singleton/Docs/Tasks/实现普通类单例.md)仍为 `NotStarted`，目前只有规划文档及元数据，缺少实际源码 API 与程序集。 | 由该任务提供可消费的普通类单例及程序集，明确受控获取、初始化和释放完成判据。仅依赖这些产物，不需等待组件单例整项结束。 |
| 日志输出 | 已有 [Log 需求](../../Log/Docs/需求.md)与[实施任务](../../Log/Docs/Tasks/实现Unity日志封装.md)，尚无真实日志入口。 | 消费 GameSDK 的静态日志实现，正常报告受其开关与最低级别控制，不反向引用 GameHub。EventBus 接入时落实报告失败的捕获、重入保护和独立兜底，不阻断后续订阅；不承诺任意故障下均记录成功。 |

接线待对齐：建议 EventBus 在连续 Play（含关闭 Domain Reload）时隔离会话并清理旧监听，真实入口及提供方尚未确定；当前 Singleton 交付未约定会话重置能力。

下一步：按[事件与回调规则](需求.md#事件与回调规则)对齐性能负载和量化目标，落实上述前置及实际接线，取得代码实施授权后推进[实现事件总线](Tasks/实现事件总线.md)。前置未齐时可继续实施方案准备，不能视为已具备完整实施条件。
